/* Author: Jeremy */
/* EUID: jek0138 */

void mergeSort(double arr[], int l, int h);
//Sorts an array with lower bound l and higher bound h

void merge(double arr[], int l, int m, int h);
//Merges 2 arrays into arr[]

void insertSort(double arr[], int arrSize);
